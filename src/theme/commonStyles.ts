import { Colors } from "./colors";

export const commonStyles = {
    shadow: {
        shadowColor: Colors.primary,
        shadowOffset: {
            width: 0,
            height: 3,
        },
        shadowOpacity: 0.27,
        shadowRadius: 4.65,
        elevation: 4,
    }
}